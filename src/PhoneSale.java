public class PhoneSale extends AbstractSale {
    // Formula: Total sales = Price * Quantity sold = ₱500.0 * 10 = ₱5000.0

    @Override
    void calculateTotal(){
        double price;
        int quantitySold;
        double totalSales;
        totalSales = price * quantitySold;
    };

}
